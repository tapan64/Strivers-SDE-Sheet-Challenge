def ninjaAndSortedArrays(arr1,arr2,m,n):
    for i in range(m + n):
        if i >= m:
            arr1[i] = arr2.pop(0)
        elif arr1[i] > arr2[0]:
            arr1[i], arr2[0] = arr2[0], arr1[i]
            arr2.sort()
    return arr1
